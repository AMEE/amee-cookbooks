# Template stamped out by Chef

start on runlevel [2345]
stop on runlevel [06]

respawn

script
  su - <%= @user %> -c "source /etc/profile.d/rvm.sh ; rvm use <%= @version %> ; cd <%= @dir %> ; unicorn --listen /tmp/<%= @app_name %>.unicorn.sock"
end script
